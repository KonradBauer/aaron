# MongoDB — w Docker użyj nazwy serwisu 'mongo' jako hosta
DATABASE_URL=mongodb://mongo:27017/aaron-dom-pogrzebowy

# Silny losowy ciąg znaków — wygeneruj np. przez: openssl rand -base64 32
PAYLOAD_SECRET=zmien-to-na-silny-losowy-ciag

# Publiczny URL strony (bez trailing slash) — potrzebny dla panelu Payload admin
NEXT_PUBLIC_SERVER_URL=https://twoja-domena.pl
