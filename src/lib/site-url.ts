/**
 * Publiczny adres strony — jedno źródło prawdy dla canonical URLi, sitemap,
 * robots i danych strukturalnych (JSON-LD).
 *
 * Placeholder do zmiany na docelową domenę klienta. Po podaniu domeny
 * wystarczy zmienić TĘ jedną linię.
 */
export const SITE_URL = 'https://twoja-domena.pl'
